#this DDL querie is for crealing the quiz tables, run this everytime you want to add a new students answers
create table DesignQuestions(
Design varchar(255),
DesignPoints varchar(1));
create table DMQuestions(
DigitalMarketing varchar(255),
DMPoints varchar(1));
create table DevQuestions(
Development varchar(255),
DPoints varchar(1));